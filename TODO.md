# TODO

## Distribution

- [ ] **Upstream quack-rs PR** — Open `killzoner/quack-rs:feat/bundled-test-prebuilt-libduckdb` against `tomtom215/quack-rs:main` so the fork pin can eventually be dropped (lets `bundled-test` link a pre-built libduckdb instead of compiling from source).

## Out of scope

- DuckDB community-extensions submission.
